1. Sanitization of all input data that will persist.

2. Verify and test that the account identifiers exist in the transaction request.

3. Include caffeine cache when retrieving accounts.

4. Create a global exception handler.

5. Include authentication in requests.

6. Use the gson and mockito libraries in the tests.

7. Replace the repository to store the accounts with H2 DB.

8. Replace the current programming paradigm with asynchronous programming using the spring-boot-starter-webflux library
